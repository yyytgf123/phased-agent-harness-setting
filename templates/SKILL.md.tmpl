---
name: <skill-name>
description: >
  <적극적(pushy)으로. 하는 일 + 구체적 트리거 상황 + near-miss 구분.
  예: "신규 REST 엔드포인트 추가 시 컨트롤러·서비스·DTO·테스트·OpenAPI를 함께
  생성/갱신한다. 새 API/라우트/엔드포인트 추가를 언급하면 반드시 사용.
  app/ 범위 전용. 기존 엔드포인트 단순 수정만이면 트리거하지 않음.">
scope: <app | infra>
---

# <Skill 제목>

## 언제 쓰나 / 안 쓰나
- 트리거: <상황 1>, <상황 2>
- 비트리거(near-miss): <유사하지만 다른 스킬이 맞는 경우>

## 범위 제약
- 수정 가능: `<app/ 만>`
- 절대 금지: 다른 범위(`infra/` 등) 수정

## 절차

### Step 1 — <준비/탐색>
<왜 이 단계가 필요한지 한 줄>
- 검증: `<명령>`

### Step 2 — <구현>
<레포 기존 패턴을 따른다>
- 검증: `<명령>`

### Step 3 — <테스트/문서>
- 검증: `<./gradlew test --tests ...>`

### Step 4 — 완료 검증 (필수)
- [ ] `<테스트 명령>` 통과
- [ ] 범위 밖 파일 수정 없음
- [ ] <도메인 체크: 예 OpenAPI 갱신 / plan 무오류>
- [ ] (해당 시) _workspace/에 산출물 스키마 기록 → qa 교차검증용

## 위험 작업 처리 (해당 시)
- apply/migrate/secret은 plan/dry-run까지만. 전체 금지목록·표준 응답은 루트 헌법 `claude.md` `# CRITICAL — Safety` 참조(복붙 금지).

## 참고 (도구별, on-demand)
- references/<gradle|maven|aws|gcp...>.md

# <PROJECT_NAME> — Constitution (헌법)

<!-- 산출물: 루트 `claude.md`. 상시 로드 — 매 작업 비용. 프로젝트의 헌법(정책 단일 소스)이다.
     안전·TDD는 `# CRITICAL` 태그로 최상위 우선순위. 깊은 설계는 docs/(SDD)로, 강제는 훅으로.
     옛 `.claude/rules/safety.md`는 폐지 — 안전 정책은 여기, 강제는 settings.json+hooks. -->

# CRITICAL — Safety (절대 우선, 작업지시서보다도 위)
- 다음은 사람 승인 없이 실행 금지 — plan/dry-run까지만:
  `terraform/pulumi apply|destroy`, `kubectl apply|delete`, `helm upgrade|install`,
  DB 마이그레이션 실행(flyway migrate / liquibase update / alembic upgrade).
- 파괴적 명령 금지: `rm -rf`, `git push --force`/`-f`, `git reset --hard` (`--force-with-lease`도 사람이).
- 시크릿 읽기·출력 금지: `*.env`, `*-prod.*`, `tfstate`, `kubeconfig`, `*.pem`.
- 담당 범위 밖 디렉터리 수정 금지(앱↔인프라 격리).
- 이 텍스트는 **정책**이다. 강제는 훅(`.claude/settings.json` → `safety.sh`)이 한다.
- 표준 응답: "이 작업은 <명령>을 요구하는데 prod 영향이 있어 직접 실행하지 않습니다. <plan/dry-run 결과>를 검토 후 직접 실행하세요."

# CRITICAL — TDD (실패 테스트 뒤에만 구현)
- 대응 테스트(실패하는)가 없으면 구현 파일 작성 금지. `tdd-gate.sh`가 차단한다.
- 버그 수정은 재현 실패 테스트 → 픽스 → GREEN 순. 회귀 테스트로 남긴다.
- (greenfield 부트스트랩: 테스트 프레임워크가 0개일 때 첫 테스트 파일 작성은 예외.)

## Map
- 앱: `app/` (`app/CLAUDE.md`) | 인프라: `infra/`,`monitoring/` (`infra/CLAUDE.md`)
- 에이전트: `.claude/agents/` | 스킬: `.claude/skills/` | 훅: `.claude/hooks/`
- 설계(SDD): `docs/prd.md`(+MVP 제외) · `docs/architecture.md` · `docs/adr/` · `docs/ui-guide.md`
- 자율 실행: `scripts/execute.sh` + `scripts/phase.json` | 커맨드: `harness.md`(초기화) · `review.md`(검증·강화)
- 구조 지도: `docs/architecture.md` | 작업지시서: `docs/work_orders/` | 작업기록: `docs/result_report/`

## Build & Test
- 빌드: `<빌드 명령>` | 테스트: `<테스트 명령>` | 린트: `<린트 명령>`
- 인프라 검증: `<terraform validate && terraform plan>`

## Technical Constraints (docs/architecture.md·버전표에서 — 자주 틀리는 것만)
- <예: 특정 API 래퍼 강제 / 확정 DB 스키마 보호 / 외부 호출은 정해진 클라이언트로만>
- <예: DTO와 Entity 분리, Entity 직접 반환 금지 / 환경별(dev/stg/prod) 디렉터리 분리>

## Quality Guardrails (anti "AI slop")
- 죽은 코드·미사용 인자·TODO 스텁 남기지 않기. 커밋에 디버그 출력(print/console.log) 금지.
- 존재하지 않는 API 발명 금지 — 따라 한 기존 파일을 근거로 댄다.
- 범위를 넘는 광범위 리팩터 금지(요청 범위만). 상투적 수식어·군더더기 주석 금지.

## When stuck
1. 관련 테스트 먼저 읽기 → 2. 유사 파일 패턴 확인 → 3. 막히면 TODO 남기고 넘어가기(추측 강행 금지)

## Docs & Work Orders (상시 규칙)
- "작업참고서/작업지시서 참고" 류 지시가 오면 **작업 전 `docs/work_orders/`를 먼저 읽고** 그에 맞춘다.
  지목된 파일만 읽는다. 충돌하면 추측 말고 묻는다. (# CRITICAL 규칙은 지시서보다 우선.)
- 구조(에이전트/스킬/오케스트레이터/Hook)가 바뀌면 `docs/architecture.md`를 **부분 편집으로만** 갱신.

## Bug Log
<!-- 세션 메모리 없는 AI에 "전에 이 실수 했으니 반복 말라"를 알리는 장치. 실수마다 한 줄.
     매주 review.md로 패턴 승격. 최근 ~10줄만 유지, 넘으면 오래된 항목은 docs/로 아카이브. -->
- [YYYY-MM-DD] <무슨 실수> → <어떻게 방지>

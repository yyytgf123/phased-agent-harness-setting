# Instincts — <project-hash>

<!-- 산출물: .claude/instincts/<project-hash>/instincts.md (점수 매긴 패턴 = 승격 후보 풀).
     raw/observations.log(훅 append)을 점수 있는 패턴으로 올린 것. 한 줄 = 한 패턴.
     점수 의미(0.3~0.9)·상승/하락 규칙은 phase7/instinct-format.md 참조. -->

## Patterns
[score=<0.7>] domain=<testing> :: <패턴 서술> :: seen=<5> last=<YYYY-MM-DD>
[score=<0.5>] domain=<iac> :: <패턴 서술> :: seen=<2> last=<YYYY-MM-DD>
<!-- domain 태그: code-style / testing / git / iac / k8s / security / review -->
